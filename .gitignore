node_modules/
dist/
.vite/
*.local

# TypeScript incremental build caches — regenerated by `npm run build`
*.tsbuildinfo

# Logos Bibelsoftware exports kept locally for provenance, deliberately NOT published.
# The Luther 1912 text itself is public domain, but these are verbatim Logos output.
# Keep them on disk; the verified text lives in src/data/verses.de.ts.
docs/source-*.html

# OS / editor cruft
Thumbs.db
.DS_Store
